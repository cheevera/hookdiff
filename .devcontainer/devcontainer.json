{
  "name": "hookdiff",
  "dockerComposeFile": "docker-compose.yml",
  "service": "frontend",
  "workspaceFolder": "/workspaces/hookdiff",
  "forwardPorts": [5173],
  "postCreateCommand": "git config core.hooksPath .githooks && cd frontend && pnpm install",
  "customizations": {
    "vscode": {
      "extensions": [
        "biomejs.biome",
        "bradlc.vscode-tailwindcss",
        "ms-azuretools.vscode-docker"
      ]
    }
  }
}
